# QR Upgrade hosted services completion

## Spec and Global Constraints
User asks to complete the remaining Google sign-in/cloud saves, hosted content, dynamic analytics, billing, native release and enterprise readiness. Existing design language and working local studio are the baseline. Use existing Cloudflare account for storage/services and Vercel web hosting. Sources/ is read-only. Never invent provider credentials, user identity, merchant/legal details, approvals, device-test evidence or certifications. Provider setup requiring account access remains explicit; finish independent implementation while waiting. User has already authorized production deployment. No charges or agreements without required authorization.

One implementation subagent at a time; root coordinates integration and provider setup. Agents do not spawn agents, do not edit outside ownership, and preserve others' changes. Keep private drafts/images out of logs/URLs. Account identity comes only from verified server sessions. Public content requires an explicit publish action. Every edit that changes QR pixels revalidates the artifact. Feature unavailable states must be truthful.

## Task 1: Dynamic link service
Ownership: new workers/qr-service/links.mjs, migration0003_links.sql, src/lib/server/links.ts, src/app/api/links/**, src/app/r/[slug]/route.ts, tests/links.test.ts. Do not modify existing worker/account/generator/UI; root wires them.
Implement owner-scoped D1 link list/create/get/update/archive states with 50 per owner and independent publish/pause. Link fields id UUID4, owner hash, slug random16base64url immutable, name1-80,targetHTTPS max2048, status draft|published|paused, createdAt,updatedAt. New links default draft; publish explicit PATCH; update target doesn't change slug. Quota includes drafts/paused. Validate public HTTPS URLs only, no credentials/control chars, localhost/private/reserved IP literals, IP obfuscation or qrupgrade.com/r loops. Never fetch destination. Worker called only after SERVICE_SECRET auth and x-qr-user from server. Export linksRequest(request,env) handling /links or /links/:id and resolveLink(request,env) for authenticated internal /resolve/:slug.
Schema dynamic_links plus link_daily_counts(slug,day,count). Resolve uses one atomic D1 batch/statement if needed, only published links. GET redirects via Next public route /r/:slug with307,no-store,no-referrer; HEAD resolves without count. Count total opens per UTCday, no IP/cookie/UA/referrer/geo/unique claim. Counts may include bots. Limit list analytics to last30days with lifetime count computed separately or allcounts aggregate. No raw event store. Public invalid/paused/notfound should safe404 or410, never leakowner. Proxy requires getAccount and sameOrigin forwrites, bounded16KBJSON5sectimeout, validatedid, fixed Worker bearer. Do not reuse 3MBcloud body unbounded. Fetchtimeout, user-safeerrors. No arbitrary publicmutation, no hiddenidentityfallback.
Meaningful tests auth/origin/owner/isolation/quota/publish states/unsafeURLs/redirectHEADcount, SQLparameterization and failure behavior. Report under skillworkspace Task1. No commits until root asks.

## Task 2: Dynamic link workspace and studio integration
Root owns UI: /links page list/detail, URL form, publish/pause, dailyopens chart/table, createQR uses publicshortlink via in-memoryEditorDraft, no destination in navigation query. Add relevant navigation and honest empty/account-gated states. Include explicit editable-destination explanation, immutable QR link, status changes. Test actual controls/screenshots. Keep QR-exportvalidation.

## Task 3: Hosted content service and builders
Ownership for backend implementer: NEW workers/qr-service/content.mjs, migrations/0004_content.sql, src/lib/content-types.ts, src/lib/server/content.ts, src/app/api/content/**, src/app/p/[slug]/assets/[assetId]/route.ts, tests/content.test.ts. Root owns worker dispatch, React UI and /p/[slug]/page.tsx. No edits to other files, commits or subagents. You are not alone; preserve concurrent edits.

Export worker contentRequest(r,env) for /content[/:id[/submissions]], assetRequest(r,env) for /content-assets[/:id], publicContent(r,env) for /public-content/:slug[/assets/:assetId|/submissions]. Caller authenticates SERVICE_SECRET first. Owner is x-qr-user validated64hex. Private CRUD requires owner on every SQL query, id UUID4, slug immutable random16base64url. No public bucket or arbitrary remote fetch. All errors user-safe {error}, no content logging.

Shared types: ContentKind='links'|'menu'|'gallery'|'document'|'form'; ContentDraft={kind,title,description,accent,items:[{title,description,price,url,assetId}],fileId,formMessage}. All strings: title1–80, description0–1000, accent6digithex, item title1–80/description0–300/price0–30/url0–2048/assetId empty orUUID, fileId empty orUUID, formMessage0–300. Max12items; exact keys, no HTML rendering. Common fields required. Links items require URL, menu items price optional/URL optional, gallery each assetId required raster, document fileId PDF required for publishing; draft can be incomplete. Form ignores items. Reuse links.mjs validateTarget for nonempty external URLs. All referenced assets must belong to owner and be ready; validate both draft save and publish. Limit structuredJSON32KiB, timed streaming parser5s. Type ContentPage={id,slug,url,draft,status:'draft'|'published'|'paused',archived,hasUnpublishedChanges,createdAt,updatedAt}; response {page} or {pages,limit:50}. POST /api/content full ContentDraft creates draft, PUT /api/content/:id replaces draft, PATCH {action:'publish'|'pause'|'archive'|'restore'}; GET list/detail. Explicit publish atomically copies validated current draft into separate published JSON, leaves edits private until republish; pause/archive immediately disables public delivery. Archive counts toward50quota, conditionalINSERT enforces quota atomically. Public response must only include currently published JSON plus slug/url, never private owner/draft/id/archive metadata. Public Worker response {page:{slug,url,content:ContentDraft}}. Export PublicContentPage={slug,url,content:ContentDraft}. Public page getter exported getPublicContent(slug,c?) returns PublicContentPage or null on404; throws on service failure so root can display temporarily unavailable.

Assets POST /api/content/assets JSON {name,mime,data} with raw base64 data, supported PNG/JPEG/WebP/PDF, max2MiB decoded/3MiB request/120name. Validate claimed MIME against magic bytes and raster dimensions1..4096/max16Mpixel; reject SVG/HTML. PDF require %PDF header and trailing EOF; treat as untrusted document, served attachment only. Asset metadata {id,name,mime,size,url?}; GET /api/content/assets lists ready owned assets, GET /api/content/assets/:id delivers owned file (for editor preview). Per-owner100assets atomically reserve before R2put; ready flag finalizes. R2 keys private random, no user-supplied paths. Recover failed write safely without deleting possibly committed objects; report cleanup limits. Public /p/:slug/assets/:assetId must recheck published/nonarchived record and asset membership within published snapshot plus ownership; edits cannot expose unpublished files. MIME set server-side, no-store,nosniff,Referrer-Policy:no-referrer,CSP sandbox/default-src none. PDFs Content-Disposition attachment with sanitized filename; raster inline. Do not follow user-supplied URLs for assets.

Forms: POST /api/content/public/:slug/submissions JSON {name,email,message,consent,website}; name optional<=80,email optional<=254 with simple validated address,message1..2000,consent musttrue,website honeypot mustempty. Only published form accepts. Canonical same-origin POST, body8KiB,time5s, request trusted IP from Vercel x-vercel-forwarded-for (ignore caller x-forwarded-for; localhost fixeddev key only in dev); HMAC(secret,'content-form:'+UTCday+':'+IP) forwarded as x-qr-network. Missing trusted prodIP fail closed. Never persist IP, UA,referrer. Worker atomic quota max5/network/hour,100/form/day,1000stored submissions/form; insertion + quota should fail atomically with no concurrency overshoot (one SQLstatement or D1batchconditional). Private GET /api/content/:id/submissions returns last100 and total, owner verified; no email sending, no export of submissions publicly. Submission fields id,name,email,message,createdAt. No inferred automatic deletion policy; retained until later explicit deletion workflow. Shortlived networkhash quota rows may cleanup after2days opportunistically, preserving current enforcement. Honest privacycopy belongs root.

Next proxies: verify getAccount, canonical sameOrigin for all private writes, explicit method/route allowlists, fixed trustedWorker bearer and ownerhash, AbortSignal15s max, bounded bodies before fetch. Public reads still require serversecret. /api/content/public/:slug/submissions publicly accessible only with origin/ratechecks above. Export contentProxy, assetProxy, publicContentAsset, publicSubmission, getPublicContent; routes concrete static assets before dynamicid. Never accept browser x-qr-user/network. Meaningful tests with real SQLite adapter (tests/links.test.ts pattern), fake privateR2: owner/isolation, draft/publish snapshot separation, pause/archive asset invalidation, type/magic/dimensions/oversize, quotas, pendingupload failure, formspam/concurrency-style boundaries, safe headers, proxy auth/origin/networktrust, service failures. Typecheck. Report contract/interfaces/test results/limits in .superpowers/sdd/2026-09-20-hosted-services/task-3-report.md. Do not run whole suite or deploy; root integrates.

## Task 4: Billing and entitlements
Ownership for new implementation agent: NEW src/lib/server/billing.ts, src/lib/billing-types.ts, src/app/api/billing/**, workers/qr-service/billing.mjs, migrations/0005_billing.sql, tests/billing.test.ts, docs/implementation/billing-setup.md. Root owns billing React screen, dependency install already done stripe22.6.2, dispatcher. One implementer at a time, no subagents/commits, preserve others' edits. Use existing getAccount and accountSameOrigin. Read current primary Stripe SDK/docs as necessary; sources https://docs.stripe.com/webhooks and https://docs.stripe.com/customer-management/integrate-customer-portal and https://docs.stripe.com/api/checkout/sessions/create. Do not enable real charges or invent legal/merchant/productdetails.

Implement real Stripe-hosted web subscriptions behind required serverenv BILLING_ENABLED='true', STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_PRO,STRIPE_PRICE_BRAND, plus configured account/service. No fallbackprices/currency or activeoffers. Two envpriceIDs distinct, validated Stripe price objects active, recurring monthly/yearly, quantity1, nonzero unit_amount, currency safe. GET /api/billing public responds BillingStatus={configured,signedIn,plans:[{id:'pro'|'brand',name,amount,currency,interval:'month'|'year',intervalCount}],subscription:null|{tier:'pro'|'brand',status,cancelAtPeriodEnd,currentPeriodEnd},canManage:boolean}. Anonymous configuredresponse may include validated activeprices. Provider failure return503, never silently mark paid subscriberfree. Missingconfig normal200 configuredfalse/plans[]/subscriptionnull/canManagefalse. Do not claim tier benefits or alter fixed freequotas; charges stay off until merchant owner confirms offers and quotas separately. UI consumer root displays actual Stripe amounts if configured.

Private Worker billingRequest(r,env) only called after sharedSERVICE_SECRET gate. D1 ownerhash=>Stripecustomer binding, unique owner and customer, no email/carddetails. Internal GET /billing owner returns state incl customerId/current checkout reservation; PUT /billing/customer binds customer only if unbound orsame (reject changed binding), owner64hex validated. Stripe customers.create idempotency ownerstable, metadata owner, emailverifiedaccountemail only as necessary. Customer IDs never accepted from browser. Keep checkout state owner-scoped, atomic lease so simultaneous requests cannot create duplicate subscription checkout even differenttier. Use /billing/checkout lease/reserve/finalize/release actions with short servergeneratedtoken, or equally safe bounded module interface; document exact routes for root dispatch. If ambiguity happens after Stripecreatedsession, preserve reservation and use Stripe idempotencykey to recover. Don't blindly release an uncertain checkout. Reuse valid open session; prevent second checkout when customer already has subscription active/trialing/past_due/unpaid/incomplete/paused; use portal for existing. Existing current subscription and entitlements read fresh from Stripe, not webhookorder or success query. Checkout return success only indicates return; server status remains authoritative. Enforce at most bounded100subscriptions listing, handle overflow/failure as503. Filter mappedprices and customerownership. No unexpected custom redirectURLs supplied bybrowser.

POST /api/billing/checkout exact JSON {tier:'pro'|'brand'}, max1KiB/timedbody5s, verifiedsession+sameorigin, Stripe subscription checkout quantity1, serverfixedprice/customer, metadataowner, canonical success https://qrupgrade.com/billing?checkout=returned and cancel https://qrupgrade.com/billing?checkout=cancelled (development origin3040 allowed). Checkout expires bounded30minutes and stripeSDK timeout10s/maxretries1. Return {url} only if https://checkout.stripe.com target (or explicit official hostedStripeURL verified). POST /api/billing/portal requires same auth/origin no customer ininput, fixed canonical return /billing, return {url} verified billing.stripe.com. Only browser redirects to these trusted serverresponses.

Webhook POST /api/billing/webhook raw bodymax256KiB/time5s, verifies Stripe-Signature with SDKconstructEvent and secret default300secondtolerance, rejects badsignature/replayexpired/bodyoversize. Handle subscription created/updated/deleted and checkout.session.completed; ignoreunneededtypes with200. Signaturevalid event not trusted to grantentitlements: persist only id/type/Stripecreated/customer/subscription identifier metadata (no fullpayloaddump), ownerbinding from persistedcustomer, uniqueeventID idempotence. Do notoverwrite freshsubscriptionstate with staleeventpayload; GET/checkout obtainStripefreshsource. RetryableWorkerfailure503 soStripe retries, neverackuntil durableeventrecorded. Worker event endpoint /billing/events fixed bearer, validateeventfields and customerbinding. Mode test/live must match configuredkey mode; noCrossModegrant. Unknowncustomer event safelyignored/acknowledged withoutcreatingowner. Document reconciliation using liveStripe checks and webhookduplicate/out-of-orderbehavior. No sessions/clientsecrets/cardinputstored in client orlogs.

Expose typed helpers for routes and dependency injection for tests. Meaningful tests SDKfixture + realSQLite: account/origin/body gates, disabledreadiness, badsignature/expiredsignature/validSDKgeneratedtestsignature, customercrossownerbinding, checkoutconcurrency/recovery, duplicates andout-of-order events, active subscription portalbehavior, configuredprice allowlist, fixedreturnURLs, networkfailure unavailable. No livefinancialtransactions. Tests/billing.test.ts+typecheck only; fullsuite root. Report task-4-report.md in current skillworkspace with exactinterfaces/endpoints/env and limitations. Setupdoc concrete providersteps, owned/legalfields explicitlypending; publicclaimsnone; no activation. Root writes UI afterexacttypesready.

## Task 5: Native release and operational readiness
Inspect realExpo/Apple/Googlebuildaccess. Prepare signedbuilds whenidentity/platformavailable; no signingidentityinvented. NativeAGENTS exactExpo57docs beforeediting. Prepare concrete releaseartifacts and devicechecklist; physicaltests requiredevices. Implement operational gaps possiblehere (private backup/recovery rehearsal, boundedretention/reconciliation, auditrecordswithoutpayloads, dataexport/deleteconfirmationflows whereappropriate). Collect remaining organizational evidence honestly and draftowner-reviewpackets, no forgedapproval. Rootrecords concretedependencyblockers.

## Task 6: Review, verification and deployment
Taskspec/qualityreviews and finalwholebranchreview. Test productionfullflows asaccesspermits withscreenshots; no claimunverifiedOAuth/payment/nativeflows. Runrequiredautomatedchecks atfinalsource. Deployverifiedweb/servicechanges using existingauthorizedproductionworkflow. Keep appusable and clarify exactly whatneedsaccountowneraction.
