# Public launch completion

## Spec and constraints
User requires public Google sign-in, working billing and all feasible enterprise readiness controls before stopping. Apple Developer enrollment/signing is explicitly deferred. Continue technical work while awaiting business facts. Never invent legal identity, payment activation, operational approval, audit certification or physical-device results. Existing production deploy is authorized. Preserve current studio/aesthetics and existing data. Sources are read-only. One implementation agent at a time; no nested agents. Root owns provider/dashboard setup and compliance evidence; workers own specified code. Baseline 5eaf753.

## Task 1: Paid entitlements with enforceable offers
Ownership: shared/plan-limits.mjs and declaration/types if required; src/lib/server/billing.ts; src/lib/billing-types.ts; src/lib/server/account.ts, links.ts, content.ts only quota propagation; workers/qr-service/cloud.mjs, links.mjs, content.mjs only quota enforcement; src/components/billing-panel.tsx; tests/billing.test.ts and new entitlement tests; docs/implementation/billing-setup.md. Preserve concurrent root edits. No provider calls, credentials, deployment, subagents or commits.

Current billing is correctly disabled and has working server-verified Stripe subscriptions but no paid benefit enforcement. Implement a small shared immutable plan catalog: free 50 cloud designs / 50 dynamic links / 50 hosted pages / 100 uploaded assets; pro 200 / 200 / 200 / 500; brand 500 / 500 / 500 / 1000. Archived and pending records still count. File and request limits, retention and public form quotas remain unchanged. Plans buy these capacity increases and existing editor/scan/export features; do not promise unlimited, API, team, SSO, extra AI credits or certification. Root will provision proposed USD12/month Pro and USD25/month Brand from the user's original product direction; actual displayed prices must still come from validated Stripe objects. Show exact limits and cancellation/downgrade behavior before checkout. No paid activation from this code change alone.

Fresh server Stripe state is authoritative. Expose a typed resolver accepting already-verified owner plus BillingConfig/BillingDeps for tests. It must reuse existing customer/mode/mapped-price validation and return free only if billing is deliberately disabled or a successfully fetched binding/subscription proves no active/trialing mapped plan. Reject configured-but-incomplete state, customer/mode ambiguity, overflow or Stripe failure with a safe unavailable error, never silently downgrade paid users. Active/trialing receives its mapped tier; cancel_at_period_end stays paid until actual end. Past_due/unpaid/paused/incomplete/expired/canceled do not grant increased quotas. Customer metadata owner and D1 binding owner must match requested owner. Validate price mappings on entitlement reads. No browser tier/cap input is trusted.

Next authenticated proxies send only the resolved tier as a server-created x-qr-plan header to the bearer-protected Worker. Resolve only where needed for capacity creation/upload, plus displayed plan information. Worker maps that exact allowlisted tier to shared limits; missing header preserves current free behavior, invalid header fails closed. Never accept arbitrary numeric caps. Enforce creation limits atomically in existing INSERT SELECT statements, with accurate limit/error responses and no concurrency overshoot. Over-limit downgrade preserves read/export/update/pause/archive rights; it blocks new creation only. A browser-forged x-qr-plan is ignored by proxies. Avoid account/billing circular imports: extraction or dynamic import at invocation is acceptable but keep modules simple and no browser imports of secrets.

Tests must exercise real SQLite quota boundaries per paid/free tier, negative/unknown tier, spoofed browser header, owner/mode/price mismatch, paid cancellation-period, unpaid states, outage and deliberately-disabled behavior, and preserving reads/updates on downgrade. Extend existing meaningful tests rather than mirroring constants. Run targeted tests and typecheck, retain output in task report; root runs full release checks later. Report exact files, interfaces, command results and limitations in the generated report path.

## Task 2a: Central account revocation and private security history
Ownership: src/lib/server/account.ts and a small extracted session module if needed; new authenticated account security API routes; new workers/qr-service/account.mjs; new migration 0006_account_security.sql; workers/qr-service/worker.mjs routing/cleanup only; account-panel component and its existing styling; focused auth/security tests plus minimal existing test fixtures and error plumbing in account consumers. No provider calls, credentials, deployment, commits, mobile changes or edits to root-owned compliance docs. You are not alone; preserve others' edits.

Implement server-checked session invalidation and a clear Account action “Sign out everywhere.” New Google callbacks create a durable account-security row or read the existing session version. Signed sessions carry the authoritative version. Every protected request checks current D1 state through the bearer-protected Worker before accepting the session; stale, malformed and legacy sessions fail closed. Existing pre-release sessions may require one fresh Google sign-in after this migration. Do not cache authorization or make account reads depend on Stripe. Service outages must report temporary unavailability, not falsely claim a successful logout or that cloud data was lost. Preserve existing PKCE, state, nonce, canonical callbacks, verified email, owner hashing and cookie controls.

“Sign out everywhere” requires a valid current session, exact same-origin POST, bounded schema, atomic version increment, and confirmation text explaining that all QR Upgrade browser sessions will end but saved designs remain. Clear current session and pending OAuth cookies only after successful revocation; reconcile client account/drafts only after success. A stolen/replayed revoked token cannot re-register itself. Only a successful verified Google callback can issue a session at the new version. Reject browser-supplied owners, versions, event identities and user headers. Document that an already running operation might finish; subsequent requests fail. No password or Google account permission changes.

Provide a private, owner-scoped security history with a deliberately small fixed event schema: successful sign-in, sign-out-everywhere, and later data-export/deletion event extension points. Store generated event ID, owner hash, fixed event type and timestamp only; no name/email/IP/user agent, tokens, provider payloads, QR content, message data or arbitrary error text. Do not log every rejected anonymous request into unbounded D1. Keep at most 100 events per owner and remove events older than 30 days on insertion and scheduled cleanup. Present this as the implemented app default, not legally approved retention or enterprise audit certification. Keep security history accessible only to the same verified owner. Expose a small allowlisted recording helper for future account actions; do not add a generic browser event submission endpoint. Retain a durable security version row independent of event retention.

All Worker routes remain protected by the service bearer. Bound request input/timeouts and histories; no R2 changes. Add meaningful tests using SQLite and production routing where practical: two sessions, revoke, both rejected, fresh verified issuance works; missing row/legacy/tamper/owner mismatch; service outage; forged header/body; CSRF; history isolation and bounded pruning; login callback failure does not create session; duplicate/concurrent version changes cannot restore old access. Update existing tests with explicit test dependencies rather than adding a production bypass. Preserve current behavior of billing entitlements and non-account artwork cookies. Run focused tests and typecheck, report exact evidence and any limitations. Do not claim Google live proof or account-provider MFA from unit tests.

## Task 2b: Bounded account data export
Ownership: new src/lib/server/account-export.ts, new authenticated src/app/api/account/export routes, new workers/qr-service/account-export.mjs and declaration if needed, worker routing, narrowly expose the reviewed currentSession helper/type if necessary; new account-export-panel component and minimal account-panel/style integration; focused export tests and docs/implementation/account-data-rights.md. No provider calls, credentials, production data, deployment, commits, mobile changes or edits to root-owned compliance docs. Preserve concurrent edits. Baseline c1572c0; Tasks1/2a are reviewed.

Implement a working signed-in Account section for downloading owner-held data. Use an explicit, documented allowlist of sections: profile (verified current session name/email), cloud design metadata including archived/pending records; dynamic links plus daily aggregate scan counts; hosted page drafts and published versions including archived; asset metadata including pending; all feedback submissions joined through owned pages, not just latest100; app billing customer linkage and minimal app billing event metadata; retained private security events. Anonymous artwork jobs use a different browser identity and must not be falsely claimed included. Explain local-only browser drafts and independent Stripe receipts are separate, with existing on-device library/billing links. Do not include service keys, session versions, cookies, OAuth credentials, R2 keys, checkout reservation/session/idempotency tokens, raw provider payloads, network quota hashes or other owners' records. Received feedback in the owner's pages is part of their export; do not expose unrelated third-party rows.

Use fixed allowlisted sections and bounded keyset pagination with stable ordering and strictly validated, section-specific cursors. Avoid offsets and unbounded all() queries. Each page has a fixed row cap and a byte cap (at most 1MiB), explicit next cursor, complete boolean, counts and generated timestamp. Do not silently truncate a record or a section. An over-size single row fails explicitly. Retained historic counts/submissions/events can exceed normal plan capacity, so do not stop at a plan cap. Queries must restrict owner at SQL level, including child tables. Changes during export are not a transactional snapshot; make that limitation clear, and do not promise exact atomic snapshot consistency. Export remains available during billing outages and downgrade; no Stripe requests required.

Provide separately authenticated downloads for each ready cloud design JSON (max3MiB) and uploaded asset (max2MiB). Resolve R2 key only from an owner-scoped row; browser supplies only fixed kind and validated UUID, never a key/path. Check readiness and actual object size/body byte bound, not only D1 metadata or Content-Length. Private attachment, no-store/nosniff, safe server-generated filename (not user content), safe fixed content type/octet-stream to avoid executing uploads. Missing/pending objects return a truthful per-file error. Manifest entries contain local authenticated application download routes, not R2 public or signed URLs. Include metadata for pending records with explicit unavailable file status, never claim all files downloaded.

All requests must use a current centrally validated account. Worker validates service bearer plus owner and session version itself for exports/files, preserving401service auth versus typed409account denial conventions. Reject browser x-qr-* headers and owner/version/body injection. JSON export starts only from exact same-origin POST with small bounded schema (section,cursor only); stream files via authenticated GET with cross-site fetch rejection. Do not put sensitive contents in querystrings, page analytics, logs or filenames. No public share links, CORS widening, temporary cloud export bucket or credential caching. Reuse reviewed AccountUnavailable behavior and preserve cookies on upstream outage. Record a fixed data_export event only for a successfully prepared export page; bound history still applies. Do not expose a browser-controlled generic security event endpoint.

UI must make the section/part workflow usable: human labels, one download action per next part, progress with completed parts, clear retry after failure, no falsely complete state, and visible file download list per asset/design manifest part. A page at a time avoids accumulating hundreds of MiB in browser memory; do not auto-trigger hundreds of browser downloads. Explain that each section and the desired original files must be saved. Clear transient export state on sign-out/unmount/account switch; do not persist exported personal data in localStorage. Use safe text rendering. Downloads should use existing controls/theme and accessible status/error handling. Do not redesign the studio.

Tests: real SQLite with two owners, actual production dispatcher/proxy path where practical; complete traversal across multiple pages with tied keys, >100feedback, archived/pending records, empty sections, per-section cursor tampering/unknown fields, ID/key/header/CSRF attacks, missing/oversized/malformed private objects, actual-byte limits, revoked/legacy sessions, D1/R2/bearer outages, no secrets in export, safe attachment headers and downgrade/billing outage independence. Avoid live data. Run meaningful focused tests and typecheck. Save report with exact files/tests/interfaces/limits and limitations to .superpowers/sdd/2026-09-20-public-launch/task-2b-report.md. No commits; root will stage only your files after review.

## Task 2c: Account closure and deletion
Execute after2d billing correction. Exact brief in this plan workspace task-2c-brief.md. Fresh same-owner Google intent, explicit destructive confirmation, authoritative billing clearance and atomic write fence, immediate public unavailability, monotonic revocation, bounded retryable D1/R2 deletion and independent minimal restoration ledger. Active/uncertain billing must block closure with truthful UI; no unauthorized subscription cancellation. Do not claim all backup/local/Stripe erasure or legal approval. Preserve real user data; tests isolated. This is a substantial data-mutation task requiring independent review.

## Task 2d: Provider-proven billing cancellation and dedicated portal
Detailed brief follows account-data work. Actual sandbox portal cancellation on 20 September returned Stripe cancel_at_period_end=false, cancel_at=1792499613, status=active and item.current_period_end=1792499613. Current app shows cancelAtPeriodEnd=false and omits scheduled-end message. Add explicit validated scheduled cancellation time to public billing status and truthful UI; preserve entitled access until authoritative terminal subscription status. Test actual modern field shape, at-period-end flag shape, earlier scheduled dates, invalid timestamps and terminal states. Do not conflate any cancel_at date with end-of-period cancellation. Also add dedicated server-only STRIPE_PORTAL_CONFIGURATION so QR Upgrade never inherits another product's default portal accidentally; validate mode, configuration and safe cancellation settings before live activation. Root provisions exact app configuration after code and owner approvals. No live billing activation during implementation.

## Task 3: Provider and operational completion
Root: finish Google production branding/policy URLs and canonical production callback; keep testing separate. Verify live Stripe merchant requirements, prepare exact products/portal/webhook and isolated sandbox proof, request only unavoidable identity/agreement/access approvals at concrete final steps. Verify actual source host/CI, least privilege/MFA evidence, redacted monitoring/test alerts, private backup/restore including D1/R2, dependency/license ownership, vulnerability-report channel, vendor/data inventory. Draft owner review packet, retain each missing external approval honestly. Do not turn manual gates green without evidence and real reviewer identity.

### Task 3 hosting decision — owner update
Owner explicitly chose moving web hosting to existing Cloudflare on20September. Preserve current live Vercel release until Cloudflare runtime, sessions, webhook verification, private caching, custom domains and edge protections are verified. Prepare migration using current vendor guidance and smallest compatible production change. Retain rollback configuration; do not remove Vercel or change DNS prematurely. New host runtime secrets stay private and must never appear in source/build output. Restricted Stripe key was created after owner verification and its exact approved scope read back. Dedicated live portal is configured; runtime/webhook connection and launch approvals remain. Any new hosting plan purchase remains a separate exact-price approval.

## Task 4: Release evidence
Scoped spec and quality review per code task, final review for combined new source. Run required automated controls once on final source, deploy authorized verified changes, exercise live public sign-in and billing without unauthorized real charges, retain screenshots. Check Android artifact/signature and feasible device tests; iOS signing remains deferred. Update readiness report with actual current scope, leaving independent auditor/owner/device-only evidence pending where unavailable.
