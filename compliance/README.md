# Enterprise readiness for QR Upgrade

This adds a control register, automated evidence, a private readiness report, and release gates to the existing web/Android/iOS product. It implements part of the evidence-and-control workflow associated with compliance platforms. It is **not a Vanta integration, SOC 2 examination, ISO certification, legal opinion or independent penetration test**.

The provisional target is SOC 2 Security and ISO/IEC 27001:2022 readiness, selected OWASP ASVS 5.0.0/MASVS controls, and privacy applicability review. The owner must confirm the legal entity, customers, audit scope and jurisdictions. Availability, Confidentiality, Privacy and Processing Integrity commitments must be scoped deliberately. HIPAA, PCI DSS, FedRAMP and other regulated frameworks are not claimed or silently added.

## Current scope

- Public Next.js site and in-browser QR editor; Google account sign-in; owner-scoped Cloudflare D1/R2 cloud designs, dynamic links and explicitly published hosted content; local Expo mobile draft flow.
- User-entered URLs, network credentials, contact details and selected images are sensitive even when only processed locally.
- A disabled-by-default server API that performs mathematical print screening. It receives no QR payload or uploaded image.
- Cloudflare Workers AI generates artwork from an explicit prompt and style. Payment collection remains disabled pending QR Upgrade-specific Stripe configuration. No visitor location analytics are active.
- Vercel web hosting, Cloudflare DNS/services, Google OAuth, Expo builds and the private GitHub repository are in scope. Provider access reviews, MFA evidence, log retention and external approvals remain separate evidence requirements. Apple Developer enrollment and device signing are deferred by the owner.

## Run the checks

Use Node.js 24 (CI target) or a compatible newer release; install both app lockfiles. Install Semgrep 1.177.0 using `scripts/security/requirements.txt`, and Gitleaks 8.30.1 from its official release. The Linux installer verifies the release checksum and installs to a caller-supplied tool directory.

```sh
npm ci
npm ci --prefix mobile
python3 -m venv /tmp/qr-upgrade-security-tools-venv
/tmp/qr-upgrade-security-tools-venv/bin/pip install -r scripts/security/requirements.txt
python3 scripts/security/install-tools.py /tmp/qr-upgrade-tools
SEMGREP_BIN=/tmp/qr-upgrade-security-tools-venv/bin/semgrep GITLEAKS_BIN=/tmp/qr-upgrade-tools/gitleaks npm run security:evidence
npm run compliance:release
```

Semgrep downloads public rules from its registry with metrics disabled. The pinned CLI version and used rule IDs are retained with the report; registry rule content can change. This is not an immutable rule-pack claim. Gitleaks scans current files locally; the CI workflow also scans Git history. A local Git history scan passed on 20 September 2026 after the verified mobile source was added to the shared repository. Future source changes require fresh checks.

Evidence is written into `compliance/evidence/`, excluded from Git, with owner-only local file permissions. Each run retains command results, hashes and scanner reports. CycloneDX inventories cover **installed npm dependencies on the runner platform**, not a finished CocoaPods/Gradle/native binary inventory. Protect CI evidence artifacts with repository access controls and export audit-period evidence to approved immutable storage; the example CI retention of 30 days is not an audit retention policy.

`readiness.html` is a private, local dashboard generated by the release check. Do not put it under `public/`, expose it as an anonymous endpoint, or upload raw internal evidence to the marketing site.

## How release is blocked

There are 14 automated controls and 19 organizational/release controls in `controls.json`.

- Technical evidence must pass, be less than 24 hours old and match a SHA-256 digest of the current first-party source/configuration. Source changes invalidate the report.
- Every expected check must appear exactly once and carry an evidence artifact whose bytes still match its recorded hash. Missing tools and incomplete reports fail closed.
- Every organizational control requires an actual evidence file, named owner, distinct named approver, review date and expiry within that control's review window. Release-specific approvals also bind to the current source digest.
- The gate exits nonzero when any requirement is absent. It never treats missing evidence as a pass or silently waives a control.
- Hashes detect change; they do not authenticate the reviewer or make evidence tamper-proof. Private storage access controls, protected CI, reviewer identity, signatures/attestations and branch protection must establish that trust.

The GitHub workflow is installed in the private `aegntic/qr-upgrade` repository. Its first remote technical run passed at55cffe6; later code changes still need a fresh run. Mandatory private branch protection was rejected by GitHub with HTTP 403 because the account needs a paid plan; do not treat the workflow alone as enforced branch protection. On pull requests/main/daily runs it enforces technical checks and retains reports. Its manual production-readiness job downloads technical evidence from the same workflow run and invokes the actual release gate. It blocks until the organization supplies protected manual evidence retrieval, establishes independent source/release approval, and connects the deployment job. GitHub Pro supports protected private branches but does not provide private environment required-reviewer gates; the proposed design is documented in `docs/implementation/provider-security.md`. There is no active deployment job or configured production environment in this project. These repository files alone cannot prevent someone with hosting administrator access from deploying outside CI; control G06 requires proving those alternate paths are restricted.

## Record approved manual evidence

Copy `manual-evidence.example.json` to `compliance/evidence/manual.json`. Add records only after an authorized review, using this schema (the values below describe fields, not an approval):

| Field | Required value |
|---|---|
| `id` | Control ID such as G01 |
| `status` | `approved`, only after the actual review |
| `owner` | Named accountable person |
| `approvedBy` | Different named authorized reviewer |
| `reviewedAt` | ISO timestamp of actual review |
| `expiresAt` | Future ISO timestamp within the control's maximum review window |
| `artifact` | Relative file beneath `compliance/evidence/`; no parent traversal or symlinks |
| `sha256` | SHA-256 of that exact evidence file |
| `sourceDigest` | Required for G14/G15/G16/G19, matching the current automated report |

Approval records are metadata, not signatures. The organization must validate reviewers and retain evidence in its approved system. Do not fabricate evidence, reviewer names, certification statuses or waivers. Controls that are not applicable need a formal scope/risk review and an approved catalog change, rather than changing their status to green.

## Operating the program

1. Approve [scope and data inventory](scope-and-data.md), [risk register](risk-register.md), and policy drafts. Assign real owners.
2. Connect the actual IdP, source host, deployment provider, DNS, device management and signing accounts. Enforce MFA, least privilege and reviewable changes there.
3. Require `technical-gate` in branch protection; install real CODEOWNERS, prevent unreviewed workflow changes, restrict deployment credentials and require authorized production reviewers.
4. Forward security event metadata to an approved log service with redaction, retention, alerts and a tested responder. Enable edge rate limiting before turning on the service API.
5. Perform the external assessment, native device tests, recovery exercise and incident tabletop. Collect actual reports and close findings.
6. Have a qualified auditor scope the SOC 2 examination / ISO certification work. Maintain operating evidence over the relevant period, not a one-time screenshot.

## Provisional framework crosswalk

These are thematic mappings for readiness, not a complete licensed control crosswalk or statement of conformity.

| Areas | QR Upgrade controls | Framework themes |
|---|---|---|
| Governance, policy, risk | G01–G03 | SOC 2 control environment/risk assessment; ISO organizational/people controls |
| Identity and workforce access | G04–G05, G13 | SOC 2 logical access; ISO access/asset/people controls |
| Secure changes and suppliers | T01–T10, T12–T13, G06, G11, G17 | SOC 2 change management/risk mitigation; ISO secure development/supplier controls |
| Operational protection and recovery | T11, G07–G10 | SOC 2 system operations; ISO monitoring, incident and continuity controls |
| Privacy and mobile lifecycle | G12, G15–G16, G19 | Applicable privacy law; OWASP MASVS storage/platform/network/privacy |
| Independent assurance and reporting | G14, G18 | SOC 2 monitoring; ISO assurance; OWASP verification evidence |

## Sources

- [AICPA: SOC services and independence](https://www.aicpa-cima.com/topic/audit-assurance/audit-and-assurance-greater-than-soc-2)
- [ISO/IEC 27001:2022](https://www.iso.org/standard/27001)
- [OWASP ASVS releases](https://github.com/OWASP/ASVS/releases) and [MASVS](https://mas.owasp.org/MASVS/)
- [European Commission: data-protection legal framework](https://commission.europa.eu/law/law-topic/data-protection/legal-framework-eu-data-protection_en)
- [OAIC: Australian Privacy Principles](https://www.oaic.gov.au/privacy/australian-privacy-principles)
- [Vanta: automated compliance](https://www.vanta.com/products/automated-compliance)

The referenced organizations describe their own standards/products. Their inclusion is not an endorsement or a certification of QR Upgrade.
